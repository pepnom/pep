
 #include <stdio.h>
 #include <stdlib.h>
 #include "element.h"
 #include "buffer.h"
 #include "tape.h"
 #include "machine.h"

   machine * createMachine(machine * mm)
   {}

   void freeMachine(machine * mm)
   {}

   wchar_t * pop(machine *);
   wchar_t * push(machine *);
   wchar_t * put(machine *);
   wchar_t * printMachine(wchar_t * sReturn, machine * mm)
   {}
   wchar_t * dumpMachine(wchar_t *, machine *);

