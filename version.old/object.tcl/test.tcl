# Tcl code generated by 'compile.tcl.pss'
source Machine.tcl; 
while { $machine(peep) >= 0 } {
  readChar;
  puts -nonewline $machine(workspace);
  puts -nonewline $machine(workspace);
  puts -nonewline $machine(workspace);
  set machine(workspace) "";
} 
